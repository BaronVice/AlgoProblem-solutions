#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;
using ll = long long;

int main(){

    cin.tie(0);
    cout.tie(0);
    ios_base::sync_with_stdio(0);

    int q;
    cin >> q;

    vector<vector<int>> memo(1e5, vector<int>(1e12, 0));

    cout << "Succesful" << '\n';


    return 0;
}